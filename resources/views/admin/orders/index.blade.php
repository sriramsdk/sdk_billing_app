@extends('layouts.admin')
@section('title', 'Order History')
@section('page-title', 'Order History')
@section('content')
<div class="mb-8"><p class="text-sm text-slate-500">All store billing activity</p><h2 class="mt-1 text-3xl font-black text-slate-900">Order History</h2></div>
<form method="GET" class="mb-6 flex flex-col gap-3 sm:flex-row"><input name="search" value="{{ request('search') }}" placeholder="Invoice, customer, or employee" class="flex-1 rounded-xl border border-slate-300 px-4 py-3"><select name="status" class="rounded-xl border border-slate-300 bg-white px-4 py-3"><option value="">All statuses</option><option value="completed" @selected(request('status') === 'completed')>Completed</option><option value="cancelled" @selected(request('status') === 'cancelled')>Cancelled</option></select><button class="rounded-xl bg-slate-900 px-5 py-3 text-sm font-bold text-white">Filter</button><a href="{{ route('admin.orders') }}" class="rounded-xl border border-slate-200 px-5 py-3 text-center text-sm font-semibold text-slate-600">Reset</a></form>
<div class="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><div class="overflow-x-auto"><table class="w-full min-w-[1050px] text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-6 py-4">Invoice</th><th class="px-6 py-4">Customer attended</th><th class="px-6 py-4">Employee</th><th class="px-6 py-4">Items</th><th class="px-6 py-4">Total</th><th class="px-6 py-4">Status</th><th class="px-6 py-4">Date</th></tr></thead><tbody class="divide-y">@forelse($orders as $order)<tr class="hover:bg-slate-50"><td class="px-6 py-4 font-bold"><a class="text-blue-600" href="{{ route('admin.orders.show', $order) }}">{{ $order->invoice_number }}</a></td><td class="px-6 py-4">{{ $order->customer?->name ?? 'Walk-in Customer' }}<div class="text-xs text-slate-500">{{ $order->customer?->email }}</div></td><td class="px-6 py-4">{{ $order->employee?->user?->name }}<div class="text-xs text-slate-500">{{ $order->employee?->employee_code }}</div></td><td class="px-6 py-4">{{ $order->items_count }}</td><td class="px-6 py-4 font-semibold">₹{{ number_format($order->grand_total, 2) }}</td><td class="px-6 py-4"><span class="rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-700">{{ ucfirst($order->status) }}</span></td><td class="px-6 py-4 text-slate-500">{{ $order->created_at->format('d M Y, h:i A') }}</td></tr>@empty<tr><td colspan="7" class="px-6 py-12 text-center text-slate-400">No orders found.</td></tr>@endforelse</tbody></table></div><div class="border-t px-6 py-4">{{ $orders->links() }}</div></div>
@endsection