@extends('layouts.employee')
@section('title', 'Dashboard')
@section('page-title', 'Daily Desk')
@section('content')
<div class="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p class="text-sm text-slate-500">Welcome back, {{ auth()->user()->name }}</p><h2 class="mt-1 text-3xl font-black text-slate-900">Your billing overview</h2></div><a href="{{ route('employee.billing') }}" class="rounded-xl bg-blue-600 px-5 py-3 text-sm font-bold text-white hover:bg-blue-700">+ Create New Bill</a></div>
<div class="grid gap-5 sm:grid-cols-2 xl:grid-cols-4"><div class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><span class="text-sm text-slate-500">Today's sales</span><div class="mt-5 text-3xl font-black">₹{{ number_format($todaySales, 2) }}</div><div class="mt-2 text-xs text-emerald-600">{{ $todayOrders }} bills today</div></div><div class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><span class="text-sm text-slate-500">This week</span><div class="mt-5 text-3xl font-black">₹{{ number_format($weeklySales, 2) }}</div><div class="mt-2 text-xs text-slate-500">{{ $weeklyOrders }} bills this week</div></div><div class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><span class="text-sm text-slate-500">This month</span><div class="mt-5 text-3xl font-black">₹{{ number_format($monthlySales, 2) }}</div><div class="mt-2 text-xs text-slate-500">{{ $monthlyOrders }} bills this month</div></div><div class="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><span class="text-sm text-slate-500">Customers attended</span><div class="mt-5 text-3xl font-black">{{ $customersAttended }}</div><div class="mt-2 text-xs text-slate-500">Unique customers today</div></div></div>
<div class="mt-6 overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><div class="flex items-center justify-between border-b px-6 py-5"><div><h3 class="font-bold">Your recent orders</h3><p class="text-xs text-slate-500">Billing activity from your account</p></div><a href="{{ route('employee.orders') }}" class="text-sm font-semibold text-blue-600">View all →</a></div><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-6 py-4">Invoice</th><th class="px-6 py-4">Customer</th><th class="px-6 py-4">Amount</th><th class="px-6 py-4">Date</th></tr></thead><tbody class="divide-y">@forelse($recentOrders as $order)<tr class="hover:bg-slate-50"><td class="px-6 py-4 font-bold"><a href="{{ route('employee.orders.show', $order) }}" class="text-blue-600">{{ $order->invoice_number }}</a></td><td class="px-6 py-4">{{ $order->customer?->name ?? 'Walk-in Customer' }}</td><td class="px-6 py-4 font-semibold">₹{{ number_format($order->grand_total, 2) }}</td><td class="px-6 py-4 text-slate-500">{{ $order->created_at->format('d M Y, h:i A') }}</td></tr>@empty<tr><td colspan="4" class="px-6 py-10 text-center text-slate-400">No bills created yet.</td></tr>@endforelse</tbody></table></div></div>
@endsection